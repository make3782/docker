server {
          listen       80;
          server_name  dev.api.55maiche.com;
          root /www/55maiche_main;


         location / {
	     if (!-e $request_filename) {
	       rewrite  ^/(.*)$  /index.php?s=$1  last;
	       break;
	    }

             index  index.html index.htm index.php;
         }

	 include "conf.d/enable-php.conf";


 }
